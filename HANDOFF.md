# HANDOFF — موقع عيادات د. إيمان العبرة

> **طريقة الاستخدام:** ارفع ملف الـ zip كامل في الشات الجديد، والصق **القسم 0** كأول رسالة. الباقي مرجع يقرأه المساعد من الملف ده.

---

## 0 — الرسالة الأولى في الشات الجديد (انسخها كما هي)

```
مرفق مشروع موقع عيادات د. إيمان العبرة (الرياض).
اقرأ أولاً: HANDOFF.md ثم content-manifest.json ثم index.html.

الوضع: خلصنا تجهيز كل الأصول والمحتوى، وبنينا بروتوتايب للصفحة الرئيسية (index.html) — شغال وكامل.
المطلوب دلوقتي: تحويله لموقع كامل بصفحات منفصلة حقيقية، static HTML/CSS/JS،
من غير framework ولا build step، يترفع على cPanel بالـ FTP.

ابدأ بمراجعة index.html وقول لي رأيك، وبعدين خطة الصفحات الباقية.
متكتبش كود قبل ما نتفق على الخطة.
```

---

## 1 — إيه اللي خلص فعلاً

| العنصر | الحالة |
|---|---|
| الصفحة الرئيسية `index.html` | ✅ بروتوتايب كامل شغال — هيرو فيديو، تيكر، ليه إحنا، مقالات، قبل/بعد، طبيبات، أجهزة، آراء، فروع، فوتر |
| الأصول (صور/فيديو/خطوط/أيقونات) | ✅ جاهزة ومحسّنة في `assets/` |
| المحتوى المنظّم | ✅ `content-manifest.json` |
| الخطوط | ✅ 9 ملفات woff2 + `assets/css/fonts.css` |
| الأيقونات و SEO | ✅ favicon متعدد المقاسات + apple-touch + manifest + OG |
| الصفحات الداخلية (12 صفحة) | ❌ لسه |

---

## 2 — الهوية

- **الاسم:** عيادات د. إيمان العبرة · Dr. Eman Alabra Clinics
- **الشعار اللفظي:** جمال يبدأ بثقة · Where Beauty Meets Confidence
- **الخبرة المعتمدة: 16 سنة** (الهوية تقول 16، اللينكتري 15، الموقع القديم 12 — **المعتمد 16**)
- **الأقسام الثلاثة:** الجلدية والتجميل والليزر · النساء والولادة · طب وجراحة الأسنان

**الألوان (بنسبها، وهي جزء من الهوية مش اقتراح):**

| اللون | الكود | النسبة | الاستخدام |
|---|---|---|---|
| بنفسجي | `#662D91` | 30% | العناوين والأزرار والهوية |
| ذهبي | `#C2A35C` | 30% | التفاصيل والفواصل والأيقونات |
| رمادي فاتح | `#D9DDE6` | 25% | الخلفيات والنصوص الثانوية |
| أسود | `#1A1A1A` | 15% | النصوص الأساسية |

**الخطوط (مركّبة وجاهزة — استورد `assets/css/fonts.css` ولا تغيّر فيه):**
- **Zain** (300/400/700/900) للعناوين — الرسمي من Google Fonts، OFL
- **IBM Plex Sans Arabic** (300/400/600/700) للمتن
- **Source Serif 4** (variable 200–900) للاتيني
- الملف فيه type scale جاهز بـ `clamp()` من `--step--1` لـ `--step-6`
- ⚠️ الخطوط في `client-originals/` **ممنوع استخدامها**: Zain هناك ناقص أوزان، و`خط ام بي سي` = **BoutrosMBCDinkum** خط تجاري، رفعه على سيرفر عام = مشكلة ترخيص

---

## 3 — الأرقام واللينكات

- **واتساب الحجز:** `966112401164` → `https://wa.me/966112401164?text=...`
- أرقام أخرى: `0583120083` · `0555474372` (قديم، محروق جوه بعض الصور)
- **إنستقرام/تيك توك:** `@dremanalabra` · **سناب:** `dremanalabraclinics` · **يوتيوب:** `@clinicdr.emannnn`
- **لينكتري:** `https://marketingdremanclinics.github.io/linktree/`
- **متجر:** `https://divmih.zid.store`
- **الموقع الحالي:** `emanclinic.com` (ووردبريس — الموقع الجديد يحل محله)
- **فيسبوك:** غير مؤكد — لم يظهر في أي مصدر رسمي

**الفروع:**

| الفرع | العنوان | المواعيد | الخريطة |
|---|---|---|---|
| القدس | مخرج 10، الطريق الدائري الشرقي | يوميًا ٩ص — ١٠م | `https://maps.app.goo.gl/Ab1t9uuchzdZj1yj8` |
| قرطبة | مخرج 8، الدائري الشمالي الفرعي، 13244 | يوميًا ١٢ظ — ٩م | `https://maps.app.goo.gl/3NTUXfLrPBw7P9ZK8` |

---

## 4 — المحتوى الجاهز (كله في `content-manifest.json`)

- **6 طبيبات:** إيمان العبرة (استشارية نساء وولادة) · إيمان عبد العزيز (أخصائية نساء وتوليد) · غادة مرسي · مي منسي · ميرفت علوش (نائب جلدية وتجميل) · لمي ورغي (طب وجراحة أسنان)
- **7 أجهزة بنصوصها:** ألثيرا بريم · مورفيوس برو · أتيفا · سبلندر إكس · ديكا · فراكشنال CO2 · بلازما ماجلان
- **5 مقالات خدمات** (نص + صورة): إبر النضارة · الشد المائي · شد الأتيفا · فيلر الشفايف · فيلر فول فيس
  - المصدر PDF في `content/articles-pdf/` — ⚠️ طبقة النص فيها تفكك في المسافات، لازم إعادة كتابة يدوية بعد الاستخراج
- **15 حالة قبل/بعد** مفصولة `-before` و `-after`
- **9 مراجعات جوجل** منقولة نصيًا (كلها 5 نجوم)، وأربعة منها تذكر طبيبة بالاسم → اعرضها في صفحة الطبيبة

---

## 5 — الأصول

```
assets/video/    intro-1080.mp4 (2.9MB) · intro-720.mp4 (1.3MB) · intro-poster.jpg
assets/img/doctors-full/    الصور الأصلية كاملة (فيها البانر والأرقام محروقة)
assets/img/doctors/         نسخة نضيفة بدون بانر  ← غير مستخدمة حاليًا
assets/img/devices-full/    الأصلية كاملة        assets/img/devices/  النضيفة
assets/img/results/         30 صورة (15 حالة × قبل/بعد)
assets/img/articles/        5 صور مستخرجة من الـ PDF
assets/img/clinic/          7 صور من جوه العيادة (فريمات من الفيديو)
assets/img/logo.png         اللوجو بخلفية شفافة
assets/icons/               favicon كل المقاسات + apple-touch + maskable + og-image
assets/fonts/ + assets/css/fonts.css
licenses/  تراخيص OFL     client-originals/  ملفات العميل الأصلية (لا تُستخدم)
```

**الفيديو:** كان HEVC (لا يعمل على كروم/فايرفوكس) → حُوّل H.264، الصوت محذوف، عمودي 9:16 مدته 11.5 ثانية. مستخدم حاليًا كخلفية للهيرو بـ `loop`.

---

## 6 — قرارات متخذة (لا تُعاد مناقشتها)

1. static HTML/CSS/JS — من غير React/Tailwind/Bootstrap/PHP/build step
2. صفحات منفصلة حقيقية بروابط، مش سكرول في صفحة واحدة
3. الهيدر والفوتر مكرّرين يدويًا في كل ملف — **ممنوع** حقنهم بـ JavaScript
4. الموشن مكتوب بإيد — ممنوع AOS/GSAP/Animate.css
5. الخطوط self-hosted — ممنوع Google Fonts CDN
6. **ممنوع اختراع أي رقم أو إحصائية** — أي رقم بلا مصدر لا يُكتب
7. ممنوع إيموجي في العناوين (موقع طبي)
8. ممنوع أي وعد بنتيجة مضمونة (ضوابط الإعلان الطبي السعودي)
9. مقبض سلايدر قبل/بعد = **إبرة تجميل SVG** مش أسهم
10. الصور تُستخدم أصلية كما هي — بدون قص

---

## 7 — قرارات معلّقة (اسأل العميل)

- [ ] **صور الطبيبات/الأجهزة:** الأصلية (فيها بانر + رقم قديم 0555474372) ولا النضيفة في `doctors/` و `devices/`؟
- [ ] **رقم الواتساب** `966112401164` هو الصح للحجز؟ (لاندنج الألثيرا بتوجّه على `0583120083`)
- [ ] تصحيح «+15 عام» في اللينكتري لتصبح 16
- [ ] لينك صفحة **Google Business** (مش لينك مراجعة مفردة) لعرض التقييم الإجمالي
- [ ] ترخيص ويب لخط Boutros MBC — لو متوفر يُضاف للنظام
- [ ] رقم ترخيص المنشأة الصحية (يوضع في الفوتر — يرفع الثقة)
- [ ] لينك فيسبوك أو حذفه من الفوتر
- [ ] **تصوير احترافي** — أهم استثمار متبقٍ. الصور الحالية فريمات فيديو موبايل وصور استوديو على خلفية بيضاء
- [ ] redirects من روابط `emanclinic.com` القديمة للجديدة (حفاظًا على ترتيب جوجل)

---

## 8 — الخطوة الجاية: الصفحات الباقية

```
index.html ✅
about.html            عن العيادات — 16 سنة، الأقسام الثلاثة، الفروع
services.html         + services/*.html لكل خدمة (5 مقالات جاهزة)
doctors.html          + doctors/*.html لكل طبيبة (مع مراجعاتها)
devices.html          + devices/*.html لكل جهاز (7 نصوص جاهزة)
results.html          15 حالة بسلايدر الإبرة
contact.html          الفرعين + الخرائط + المواعيد
booking.html          واتساب + فورم
404.html · sitemap.xml · robots.txt · .htaccess
```

**مطلوب في كل صفحة:** `dir="rtl"` · logical properties مش left/right · meta + OG · schema.org (`MedicalClinic` للعيادة، `Physician` لكل طبيبة) · صور `loading="lazy"` بأبعاد محددة · نافيجيشن 6 عناصر كحد أقصى · اختبار على 360 و768 و1440 بدون أي overflow أفقي.

---

## 9 — تشغيل المشروع محليًا

**VS Code:** افتح فولدر `out` (مش الملف) ← اضغط **Go Live** في الشريط الأسفل.
**أو من الترمنال:** `python -m http.server 8000` ثم `localhost:8000`

⚠️ لا تفتح الملف بدبل كليك — المتصفح يمنع تحميل الخطوط من `file://`.

**الرفع على cPanel:** ارفع محتويات `out` (مش الفولدر نفسه) في `public_html` ← فك الضغط ← فعّل SSL.


---

## 10 — تحديث V2 (٢١ أغسطس ٢٠٢٦) — لمسات UI/UX

كل ده متطبّق في `index.html` مباشرة. مفيش مكتبات جديدة، ولا build step، ولا ملفات إضافية.

**نظام الشكل (Shape language)**
- كل الكروت والصناديق: زاوية البداية-أعلى وزاوية النهاية-أسفل مقوّستين `26px`، والزاويتين التانيتين شبه حادة `5px` — متكتبة بـ logical properties (`border-start-start-radius`…) فتنقلب صح في RTL. التوكنز: `--rad-lg` · `--rad-sm` · `--rad-side`.
- منحنى جاي من الجنب على السكشنات الملوّنة (`#depts` · `#reviews` · `#branches`) بـ `--rad-side`.

**زخارف (Ornaments)**
- `.shead__line` بقى زخرفة SVG مضمّنة بدل الخط المستقيم، وبتتغيّر بالـ attribute: `data-o="spark|drop|needle|wave|tooth"` — الإبرة لسكشن النتائج، القطرة للمقالات، الموجة للأجهزة، وهكذا.
- `.uline` = خط منحني ذهبي تحت كلمة واحدة (مستخدم تحت «بثقة» و«والخدمات»).

**الفوتر**
- حرف علوي Zigzag (SVG مضمّن بلون السكشن اللي فوقه `#662D91` — **لو غيّرت لون سكشن الفروع لازم تغيّر اللون ده في `footer::before`**).
- فاصل Zigzag ذهبي فوق سطر الحقوق.
- `.foot__deco` = ٧ عناصر SVG خطية ثابتة (إبرة، قطرة، بريق، سنّة، وجه) بشفافية ١٤٪ — **ثابتة بدون أي حركة** بالطلب.

**عناصر جديدة**
- سكشن `#depts` — أربعة أقسام (جلدية وتجميل · ليزر · نساء وولادة · أسنان). صورة كل كارت دلوقتي أيقونة خطية مؤقتة، وفيه تعليق `<!-- SWAP -->` في كل كارت مكان تصميم الدرايف (نسبة 5:4، WebP عرض 800px).
- بحث في الهيدر (`#srch`) — يبني الفهرس **من الـ DOM نفسه** (الأقسام، المقالات، الطبيبات، الأجهزة، الحالات، الفروع)، فأي محتوى جديد يتفهرس تلقائيًا. بحث عربي مع تطبيع (ال أ/إ/آ، ة/ه، ى/ي، التشكيل). كيبورد: أسهم + Enter + Esc + Ctrl/Cmd+K.
- زرار واتساب بقى **تاب لاصق على جنب الشاشة** بدل الدايرة تحت.
- فيديو الهيرو: `autoplay muted loop playsinline` + **fade-in** عند `playing` (البوستر خلفية للـ `.hero__bg` فمفيش لحظة فاضية)، وفيه fallback بعد 2.6 ثانية.
- موشن خلفية هادي: `.glow` — دايرة ضوء ذهبية بتتحرّك بـ `transform` في 52 ثانية داخل `#reviews` و`#branches` فقط. متعطّلة تلقائيًا مع `prefers-reduced-motion`.

**النافيجيشن** رجعت ٦ عناصر (الأقسام · المقالات · قبل وبعد · الطبيبات · الأجهزة · الفروع)، و«لماذا نحن» و«الآراء» في الدروار والفوتر.

**ناقص من العميلة عشان نكمّل:**
- [ ] تصميمات/صور الخدمات من الدرايف (جلدية · ليزر · نساء وولادة · أسنان) — 5:4
- [ ] نصوص المقالات الكاملة عشان تتحط في صفحات `services/*.html`
- [ ] تأكيد قائمة خدمات قسمي النساء والولادة والأسنان (مكتوب حاليًا الحد الأدنى المؤكد بس)

**إصلاحات ٢١ أغسطس (بعد مراجعة العميلة):**
- اللوجو كان بيطلع بيضاوي: الصورة فيها `height="120"` والعرض بيتصغّر بالـCSS، ومفيش `height:auto` في الريسِت. اتضاف `img{height:auto}` عام + `aspect-ratio:1` على `.hdr__logo`. وانضاف الاسم مكتوب جنب اللوجو من عرض 1100px لأن الدايرة لوحدها مش مقروءة في 44–58px. **الحل النهائي = اللوجو الأفقي (lockup) من ملف الإلستريتور.**
- الفيديو ما كانش بيشتغل لوحده: الفallback القديم كان **بيستبدل الفيديو بصورة** أول ما `play()` ترفض. اتشال، وبقى `preload="auto"` + محاولات تشغيل متكررة على أحداث التحميل + محاولة عند أول تفاعل (pointerdown/touchstart/scroll) + إعادة محاولة عند الرجوع للتاب. سفاري في وضع توفير الطاقة بيمنع التشغيل التلقائي نهائيًا — في الحالة دي البوستر بيفضل ظاهر.

---

## 11 — تحديث V3 (٢١ أغسطس ٢٠٢٦) — تحرير بصري + مراجعة UI/UX

**رؤوس السكشنات** بقت تحريرية بدل التوسيط: خط شعرة فوق، الكيكر والعنوان على جهة البداية، والوصف على جهة النهاية متحاذي مع قاعدة العنوان (`grid-template-areas`). العنوان كبر لـ `clamp(1.9rem,7.4vw,3.5rem)`.

**صور الطبيبات والأجهزة** اتبدّلت من `doctors-full/` و `devices-full/` إلى النسخ النضيفة `doctors/*.webp` و `devices/*.webp` (مقصوصة بخلفية شفافة). ده بيحل ٣ مشاكل مرة واحدة: البانر البنفسجي المحروق في الصورة، **الرقم القديم `0555474372` اللي كان ظاهر جوه صور الطبيبات**، وتكرار الاسم مرتين (مرة في الصورة ومرة في HTML).

**كروت الأجهزة** فيها الاسم اللاتيني كطبقة شبح خلف الصورة (`content:attr(data-en)` بشفافية ٩٪) — تفصيلة تحريرية بدون أي صور إضافية.

**تباين الذهبي:** `#C2A35C` كنص على أبيض = 2.3:1 (راسب في AA). اتضاف توكن `--gold-ink:#8A6E23` (≈4.6:1) للنصوص الذهبية على الخلفيات الفاتحة، والذهبي الفاتح فضل للخطوط والزخارف وللنصوص فوق الخلفيات الغامقة.

**جديد:** شريط تقدّم قراءة في الهيدر · حالة نشطة في النافيجيشن (scroll-spy بـ `requestAnimationFrame`) · اقتباس تحريري في كروت الآراء · منحنى بين الهيرو والتيكر · أهداف لمس 44×44 لكل الأزرار الأيقونية.

**نتيجة الفحص** (بدون أي تحايل على سياسة التشغيل التلقائي): 375 · 768 · 1024 · 1440 → صفر تمرير أفقي، صفر خطأ في الكونسول، الفيديو بيشتغل لوحده، السكشن النشط والشريط شغالين.

---

## 12 — تحديث V4 (٢١ أغسطس ٢٠٢٦) — إيقاع الموشن والنهايات

- **السطر الدوّار تحت «جمال يبدأ بثقة» اتشال.** كان `animation:rot 12s steps(1)` — `steps(1)` معناها قفزة بدون تدرّج، فبيحس إنه معلّق. مكانه: سطر واحد ثابت + ثلاث «شيبس» للأقسام الثلاثة.
- **التيكر المتحرك (ديكا/ألثيرا…) اتشال بالكامل** ومعاه الـJS بتاعه. مكانه `.strip2` — شريط بيانات **ثابت**: ١٦ سنة خبرة · فرعان في الرياض · فريق طبي نسائي · أجهزة معتمدة، بفواصل ماسية ذهبية وحافة علوية مقوّسة. ثابت = مفيش حاجة تلف، ومفيش حاجة للـpause control اللي بيتطلب مع المحتوى الدوّار.
- **سكشن الفروع** بقى تدرّج بنفسجي عميق (`#4C2277 → #241033`) بدل البنفسجي المسطّح، والكروت زجاجية بحافة ذهبية عند الهوفر.
- **الفوتر اتعمل من جديد والزجزاج اتشال:** حافة علوية مقوّسة من الجنب (نفس لغة الأشكال) + هالة بنفسجية خفيفة + بلوك حجز في الأعلى + ٤ أعمدة (الموقع · الفروع بمواعيدها · تواصلي) + سطر سفلي بسطرين. الزخارف الخطية الثابتة فضلت بس بقت ٤ بشفافية ١٠٪.
- **موشن موحّد:** توكن `--out:cubic-bezier(.16,1,.3,1)` لكل الظهور (deceleration عند الدخول)، ومدة 800–900ms، وكل عناوين `h2` بتتكشف من خلف قناع (`overflow:hidden` + `translateY(105%)`). كل ده `transform/opacity` بس، ومتعطّل بالكامل مع `prefers-reduced-motion`.
- توحيد الأرقام العربية-الهندية في بيانات الفروع والشريط، وإصلاح انقلاب `@dremanalabra` في RTL بـ `dir="ltr"`.

**فحص:** 375 · 768 · 1024 · 1440 → صفر تمرير أفقي، صفر خطأ كونسول، الفيديو ذاتي التشغيل، شريط التقدّم والسكشن النشط شغالين.

---

## 13 — تحديث V5 (٢٤ أغسطس ٢٠٢٦)

- **اللهجة:** كل النصوص العامية اتحوّلت لفصحى (لماذا / شاهدي / ما هو ولمن يُناسب…). القاعدة من دلوقتي: **فصحى فقط** — العيادة خليجية.
- **أرقام الهيرو:** علامة `+` على كل رقم (`data-plus="1"` والعدّاد بيكتبها)، وعدد الأجهزة بقى **18+** بدل 7. ⚠️ المعروض في سكشن الأجهزة لسه ٧ أجهزة بصورها — محتاجين صور وأسماء الـ١١ الباقيين.
- **واتساب:** الأيقونة كانت فقاعة رسالة عامة — اتبدّلت بشعار واتساب الحقيقي.
- **شريط البيانات:** خبرة تفوق الـ١٦ سنة · فرعان · أكثر من ١٨ جهازًا · ثلاثة أقسام.
- **الأقسام:** كل كارت بقى له صورة خلفية + تدرّج بنفسجي فوقها، والأيقونة الخطية ذهبية فوق الصورة. عدد الإجراءات زاد (٦ للجلدية، ٦ لليزر).
- **الأطباء والأجهزة:** الكروت صغرت (`clamp(150px,44vw,182px)`)، وبقى كل كارت **قابل للفتح**: كليك أو Enter يفتح نافذة تفاصيل فيها الصورة والاسم والتخصص/الوصف وقائمة الاستخدامات وزرار حجز واتساب + رابط صفحة الجهاز. النافذة فيها Esc وإغلاق بالخلفية وإرجاع التركيز للكارت.
- **سلايدر الآراء:** الكارت الأوسط بيكبر ويوضح والباقي بيصغر ويبهت (`.mid` بتتحسب من مركز الريل عند كل سكرول).
- **عنصر خلفية متحرك:** `.gem` جوه سكشن الأقسام — بيطفو ببطء وبيتحرك ويلف مع السكرول (`--gy` و`--gr` من `requestAnimationFrame`). **مؤقت** لحد ما توصل صورة العميلة — التعليمات جوه الكود عند `<!-- SWAP -->`.

**المطلوب من العميلة لعنصر الخلفية:** صورة **PNG بخلفية شفافة**، عرض ٢٠٠٠px، من غير أي ظل أو خلفية بيضا. (فيديو مش هيفيد: الفيديو الشفاف مش مدعوم على كل المتصفحات — WebM alpha مايشتغلش على سفاري، وHEVC alpha مايشتغلش على كروم.) لو العنصر فيكتور، ملف SVG أحسن وأخف.

---

## 14 — تحديث V6 (٢٤ أغسطس ٢٠٢٦) — لوحة لون واحدة + سلايدر آراء + نموذج حجز

- **انتهت فكرة «كل سكشن بلون».** الصفحة كلها بقت **لوحة واحدة متدرّجة** على `body`: أربع هالات شعاعية (بنفسجي وذهبي) فوق تدرّج طولي `#FBFAFD → #F5F1F9 → #FAF6F1 → #F2EDF7 → #FAF8FC`. كل السكاشن `background:transparent`، والكروت بيضاء/زجاجية فوق اللوحة. اللمسة الغامقة الوحيدة: شريط البيانات تحت الهيرو + الفوتر.
- **سلايدر الآراء:** الكرت الأوسط أبيض ومرفوع، والكروت الجانبية مائلة `rotateY(±9deg)` وباهتة، وأفاتار دائري بحرف الاسم فوق حافة الكرت، ونقاط تنقّل تحته (النقطة النشطة تستطيل ذهبية). كل ده `transform/opacity` بس.
- **نموذج حجز جديد `#booking`** قبل الفوتر: الاسم · الجوال · الخدمة · الفرع · وقت التواصل. **بدون باك إند** — بيتحقق من المدخلات ويبني رسالة واتساب منسّقة ويفتحها. (بلوك الحجز المكرر اتشال من الفوتر.)
- **كروت الطبيبات** بخلفيات متدرّجة من ألوان الهوية (ليلكي/ذهبي) وبتتبدّل كل ٣ كروت.

---

## 15 — تحديث V7 (٢٤ أغسطس ٢٠٢٦) — إعادة ترتيب الصفحة على تسلسل الموقع المرجعي

الترتيب الجديد: هيرو (فيديو) → شريط البيانات → **عنا** → لماذا نحن → خدماتنا (الأقسام) → **العروض** → أطباؤنا → الأجهزة → قبل/بعد → المقالات → آراء العملاء → الفروع → حجز موعد → الفوتر.

- **قسم «عنا» جديد:** صورة عمودية من العيادة + شارة «١٦+ سنة من الخبرة» معلّقة على الصورة + نص تعريفي + ثلاث نقاط + زر.
- **قسم «العروض» جديد:** شبكة ٦ بطاقات (صورة مربّعة + اسم + سعر + زر استفسار عبر واتساب). **الأسعار غير موجودة في أي ملف** — كل بطاقة مكتوب فيها «السعر عند الاستفسار» وفيها تعليق `<!-- SWAP -->` مكان السعر الحقيقي.
- **عدّاد شرائح في الهيرو** (01 / 03) بنفس أسلوب المرجع.
- **عنصر الخلفية (`.gem`)** بقى **طبقة ثابتة للصفحة كلها** (`position:fixed`) بتتحرك وتلف مع نسبة التمرير في الصفحة كاملة بدل ما كان جوّه سكشن واحد.
- النافيجيشن اتظبطت على نفس تسمية المرجع: عنا · خدماتنا · العروض · أطباؤنا · الأجهزة · احجزي.

⚠️ **صور بطاقات العروض مؤقتة** — مستخدم فيها صور المقالات (قبل/بعد) لحد ما توصل صور العروض الحقيقية بأسعارها.

---

## 16 — تحديث V8 (٢٤ أغسطس ٢٠٢٦) — هيرو المرجع + بنفسجي أكثر

- **الهيرو اتنضّف:** الفيديو ملء الشاشة (`100svh`) + كيكر + العنوان + زرّان + عدّاد الشرائح فقط. اتشال السطر التعريفي وشيبس الأقسام.
- **شريط الأرقام** اتنقل من جوّه الهيرو للشريط البنفسجي تحته (١٦+ · ٣+ · ١٨+ · ٢+) بفواصل رفيعة.
- **زر الواتساب** بقى دائرة خضراء عائمة أسفل الشاشة بحلقة نبض خفيفة (بدل التاب الجانبي)، في الجهة المقابلة لعدّاد الهيرو عشان ما يتراكبوش.
- **البنفسجي زاد:** تدرّج الصفحة بقى بنفسجي أوضح، طبقة الهيرو بنفسجية أقوى، وكل الكيكرز (`.shead__k`) بنفسجية بدل الذهبي، وأسعار العروض ولينكات المقالات والأجهزة بنفسجية.
- **بطاقات الطبيبات:** أصغر، الصورة بنسبة 3:4 على تدرّج ليلكي/ذهبي، الاسم والتخصص في المنتصف، وتحتهم «الملف الشخصي» — والضغط يفتح نافذة التفاصيل.

---

## 17 — تحديث V9 (٢٤ أغسطس ٢٠٢٦) — بعد مراجعة تسجيل الشاشة للمرجع

- **رجعنا لترتيبنا:** عنا · لماذا نحن · خدماتنا · المقالات · قبل وبعد · الطبيبات · الأجهزة · العروض · الآراء · الفروع · حجز موعد. (ترتيب المرجع اتلغى.)
- **بطاقات الطبيبات** بقت بألوان العيادة زي المرجع بالظبط: كارت بتدرّج بنفسجي غامق، الصورة المقصوصة فوقه، الاسم أبيض والتخصص ذهبي فاتح، وتحتهم «الملف الشخصي». وعند الهوفر **الكارت بينقلب لفاتح كريمي** والنص بيبقى بنفسجي — نفس سلوك المرجع.
- **الآراء داخل لوحة ملوّنة** بحواف مقوّسة (خوخي → ليلكي) زي لوحتهم، والبطاقات الجانبية **مايلة ±4.5°** (دوران ثنائي الأبعاد مش ثلاثي) والوسطى بيضاء مرفوعة، والنقاط تحتها.
- **الجوهرة** بقت أكبر (حتى 720px) وأوضح (شفافية .44) وبطبقات زجاجية وخطوط قطعية بيضاء — أقرب لشكل الجوهرة عندهم لحد ما توصل صورة العميلة.
- **ختم اسم العيادة** بالحروف اللاتينية في أسفل الفوتر بشفافية ٥٪ — نفس فكرة `CLARITY BEAUTY` عندهم.

---

## 18 — تحديث V10 (٢٥ أغسطس ٢٠٢٦) — ملفات الطبيبات

- **سبب اختلاف ألوان بطاقات الطبيبات:** كانت في قواعد قديمة من V6 بتحط تدرّج فاتح على `img` نفسها (`:nth-child(2n)` و `:nth-child(3n)`) وبتغطي لون الكارت. اتشالت كلها، وكل الصور بقت `background:transparent`، فالبطاقات الستة كلها بنفسجية دلوقتي.
- **ملف الطبيبة:** الضغط على أي بطاقة بيفتح عرضًا كامل الشاشة على أسلوب كروت «طبيبك» بتاعة العيادة: صورة دائرية بإطار بنفسجي، الاسم بالبنفسجي مع الشرطة الذهبية، التخصص، ثم صندوق «الخبرات والشهادات» بقائمة نقطية، وزر حجز عبر واتساب باسم الطبيبة.
- **بيانات الطبيبات الستة** اتكتبت من كروت العميلة (`DOCBIO` في الجافاسكربت) — والأسماء والتخصصات في `content-manifest.json` اتظبطت عليها: لمياء ورغي (مش لمي)، ميرفت علوش «نائب أمراض جلدية وتجميل»، إيمان عبد العزيز «أخصائية النساء والتوليد».
- ⚠️ لما نقسّم الموقع لصفحات حقيقية، محتوى `DOCBIO` ده هو اللي هيروح مباشرة في `doctors/<slug>.html`.

---

## 19 — تحديث V11 (٢٥ أغسطس ٢٠٢٦) — صور الأقسام + الوضع الليلي

- **صور الأقسام الأربعة** من العميلة (أبيض وأسود بخلفية سودة وأقواس بنفسجية) في `assets/img/depts/`:
  `dermatology.webp` (وجه جانبي) · `laser.webp` (ساقان) · `obgyn.webp` (حمل) · `dentistry.webp` (ابتسامة).
  الطبقة اللي فوق الصورة اتخفّفت (الصور غامقة أصلًا) والأيقونة الخطية اتشالت من كروت الأقسام عشان الصورة تتنفّس.
- **زر الوضع الليلي** في الهيدر جنب البحث (قمر/شمس). الثيم بيتخزن في `localStorage` (جوّه try/catch عشان `file://`)، ولو مفيش اختيار محفوظ بيتبع `prefers-color-scheme` بتاع الجهاز. `<meta name="theme-color">` بتتغيّر مع الثيم.
- **تطبيق الثيم** بـ `html[data-theme="dark"]` وتوكنز مقلوبة: `--paper:#150F1F` · `--white:#1D1529` · `--ink:#F2EDF8` · `--grey:#2C2239`، وتدرّج الصفحة بقى بنفسجي غامق. كل الأسطح البيضاء (كروت، لوحة الآراء، فورم الحجز، النوافذ، الدروار، الهيدر الصلب، حقول الإدخال) لها بدائل غامقة، والنصوص الرمادية بقت `#C9C0D8`.
- **انتقال ناعم** بين الوضعين (`background-color/border-color/color` في 450ms) ومتعطّل مع `prefers-reduced-motion`.

---

## 20 — تحديث V12 (٢٥ أغسطس ٢٠٢٦) — إصلاحات الوضع الليلي

ثلاث مشاكل ظهرت في الوضع الليلي واتصلحت، وكل واحدة كان ليها سبب مختلف:

1. **الهيدر كان بيفضل أبيض** — الخلفية البيضا مش على `.hdr` نفسه، هي على `.hdr::before` بقيمة `rgba(255,255,255,.92)`. أوفررايد الوضع الليلي كان بيستهدف `.hdr.solid` فما كانش بيلمسها. الحل: `html[data-theme="dark"] .hdr::before` + تفتيح لون روابط النافيجيشن واسم العيادة.
2. **كروت الفروع كانت فاتحة** — القاعدة القديمة `#branches .br__c` فيها **ID**، ونوعية الأولوية (specificity) بتاعتها أعلى من `html[data-theme="dark"] .br__c`، فكانت بتغلبها. الحل: أوفررايد بنفس التخصيص `html[data-theme="dark"] #branches .br__c` مع تلوين العناوين والأسطر والأزرار.
3. **قوائم الاختيار في فورم الحجز** — عناصر `<option>` بترسمها المتصفح نفسه: الخلفية بتفضل بيضا والنص كان بياخد اللون الفاتح من `select` فمكانش باين. الحل: `color-scheme:dark` على الحقول + تلوين `option` و`option:checked` صراحةً.

**قاعدة عامة للي جاي:** أي أوفررايد للوضع الليلي على قاعدة أصلها فيها `#id` لازم يتكتب بنفس التخصيص أو أعلى، وأي خلفية متكتوبة في `::before/::after` لازم تتعمل لها نسخة غامقة منفصلة.

---

## 21 — تحديث V13 (٢٥ أغسطس ٢٠٢٦) — فيديو الهيرو: تشخيص وإصلاح

**الملف نفسه سليم:** `intro-720.mp4` — H.264 High profile, level 3.1, yuv420p، وذرّة `moov` في أول الملف (faststart) — يعني مدعوم على كل المتصفحات ويبدأ التشغيل قبل اكتمال التحميل.

**سببان كانا يمنعان ظهوره:**
1. **طبقة اللون فوق الفيديو كانت تقيلة جدًا** (`rgba(42,16,54,.62)` فوق و`.78` تحت + هالة بنفسجية). متوسط سطوع منطقة الهيرو كان 68/255. اتخفّفت لـ `.46 / .14 / .72` بهالة أهدى → السطوع بقى 96/255 والفيديو باين.
2. **في ملف المعاينة، الفيديو كان مضمّن كـ `data:` URI.** سفاري على iOS **بيرفض** تشغيل الفيديو من `data:` URI لأنه بيحتاج دعم byte-range. الحل: الفيديو بقى بيتحوّل وقت التشغيل من base64 لـ **Blob URL** (`URL.createObjectURL`) — وده سفاري بيتعامل معاه عادي.

**اتضاف كمان:** مصدر `intro-540.webm` (VP9) كبديل تلقائي للمتصفحات اللي مافيهاش H.264، ونفس البديل داخل ملف المعاينة (بيتفعّل تلقائيًا لو الـmp4 فشل).

**نتيجة الاختبار** (بسياسة التشغيل التلقائي الافتراضية، بدون أي تدخل من المستخدم): الفيديو يبدأ لوحده، ويستمر (1.5s → 4.5s → 7.5s)، **ويرجع لأوله ويكمّل** (loop) — وفرق البكسل بين لقطتين بفارق ثانيتين ونصف = 41، يعني حركة حقيقية مش صورة ثابتة.

⚠️ **الحالة الوحيدة اللي مافيش حل ليها:** آيفون في وضع توفير الطاقة (Low Power Mode) بيوقف التشغيل التلقائي لكل الفيديوهات على مستوى النظام. في الحالة دي البوستر بيفضل ظاهر مكان الفيديو ومفيش مربع أسود.

---

## 22 — تحديث V14 (٢٥ أغسطس ٢٠٢٦) — سبب عدم ظهور الفيديو في ملف المعاينة

الكلام اللي فات عن `data:` URI كان **نصّه غلط**. بعد اختبار مقارن حقيقي:

| الطريقة | يشتغل؟ | يظهر على الشاشة؟ |
|---|---|---|
| `Blob URL` من base64 | ✅ `currentTime` بيتقدّم و`readyState=4` | ❌ **لا** — المتصفح مابيرسمش الإطارات، الصورة تفضل ثابتة |
| `data:` URI في `<source>` | ✅ | ✅ نعم (من http ومن file:// الاتنين) |

يعني الـBlob اللي حطيته «كإصلاح» هو اللي كان مخلّي الفيديو شغال بس الصورة ثابتة — وده بالظبط اللي العميلة كانت شايفاه.

**الوضع النهائي في ملف المعاينة:** مصدرَان `data:` URI — `mp4` (H.264) وبعده `webm` (VP9) — من غير أي Blob.

**مشكلة تانية اتصلحت:** اختيار المصدر كان بيفشل أحيانًا وينتهي بـ `networkState === 3` (NO_SOURCE) — سباق تحميل مع ملفات base64 الكبيرة (٥.٨ ميجا). اتضاف **حارس** بيعيد `load()` كل ١.٢ ثانية لحد ٨ محاولات لو `readyState` فضل صفر.

**زر تشغيل احتياطي** في زاوية الهيرو بيظهر **فقط** لو الفيديو ما اشتغلش بعد ٣ ثوانٍ (لوضع توفير الطاقة في iOS مثلًا)، وبيختفي أول ما يشتغل.

**نتيجة الاختبار النهائي:** ٥ تشغيلات متتالية (http/ديسكتوب و file/موبايل بالتبادل) — ٥ من ٥ نجحت: `readyState=4`، الفيديو شغال ومكرَّر، وفرق البكسل بين لقطتين بفارق ١.٤ ثانية بين ٤٢ و٦٢ = حركة حقيقية على الشاشة.

---

## 23 — تحديث V15 (٢٦ أغسطس ٢٠٢٦) — السبب النهائي لعدم ظهور الفيديو في ملف المعاينة

صفحة الفحص أعطت الإجابة القاطعة على جهاز العميلة (كروم/ويندوز):

| المؤشّر | النتيجة |
|---|---|
| الجافاسكربت | ✅ يعمل |
| دعم الترميز | ✅ H.264: probably · VP9: probably |
| خطأ الفيديو | لا يوجد |
| **حالة الشبكة** | **3 = لا يوجد مصدر صالح** |
| **المصدر المستخدَم** | **لم يُختَر مصدر** |
| داخل إطار مضمّن | **نعم** |

المتصفح يدعم الترميز ولم يُبلّغ عن خطأ، ومع ذلك **رفض كل المصادر** — وهذا التوقيع المميّز لـ **Content-Security-Policy** يمنع `media-src` من `data:`. عارض الملفات يفتح الصفحة داخل `iframe` عليه سياسة تسمح بالصور (`img-src data:`) وتمنع الوسائط.

**الخلاصة: أي `<video>` مضمّن داخل ملف المعاينة لن يعمل في ذلك العارض مهما كانت الصيغة (mp4 أو webm أو blob أو data).** المشكلة في بيئة العرض لا في الموقع.

**الحل المطبَّق (في نسخة المعاينة فقط):** عنصر `<video>` يُستبدل أثناء توليد المعاينة بـ **WebP متحرك** بنفس المشهد (10 إطارات/ث، عرض 380px، ‏1.1 ميجا) — الصور مسموحة فيعمل ويتحرك ويتكرّر داخل العارض. الملف `assets/video/hero-anim.webp`.

⚠️ **ملف المشروع `index.html` لم يتغيّر:** فيه `<video>` الحقيقي بمصدرَي mp4 و webm، وهو ما سيُرفع على الاستضافة. الـWebP المتحرك أداة معاينة فقط ولا يُستخدم في الموقع النهائي (حجمه أكبر وجودته أقل من الفيديو).

---

## 24 — الرفع على GitHub Pages

**نعم، الفيديو سيعمل.** القيد الذي منعه في ملف المعاينة كان سياسة أمان في عارض الملفات داخل التطبيق، ولا وجود له على GitHub Pages: الموقع يُقدَّم عبر https من نطاق عادي، والفيديو ملف مستقل يُطلب بشكل طبيعي مع دعم byte-range، فيبدأ التشغيل قبل اكتمال التنزيل.

**خطوات الرفع:**
1. أنشئي مستودعًا جديدًا (Public).
2. ارفعي **محتويات مجلد `out` نفسها** — أي `index.html` و`assets/` وباقي الملفات — في **جذر المستودع**، لا مجلد `out` نفسه. (لو رفعتِ المجلد، ستكون الصفحة على `/out/` وستنكسر المسارات.)
3. Settings ← Pages ← Source: `Deploy from a branch` ← Branch: `main` ← Folder: `/ (root)` ← Save.
4. انتظري دقيقة إلى دقيقتين ثم افتحي الرابط.

**نقاط مهمة:**
- ملف `.nojekyll` مضاف في جذر المشروع — يمنع Jekyll من معالجة الملفات وتجاهل أي مجلد يبدأ بـ `_`.
- **حساسية حالة الأحرف:** GitHub Pages يفرّق بين الكبير والصغير في أسماء الملفات (على عكس ويندوز). كل المسارات عندنا بحروف صغيرة، فلا تغيّري أسماء الملفات عند الرفع.
- حدود GitHub Pages: أقصى حجم ملف واحد 100 ميجا، وحجم الموقع المنشور حتى 1 جيجا. الفيديو عندنا 1.3 ميجا والمشروع كله ~20 ميجا — بعيد جدًا عن الحدود.
- الدومين المخصص: Settings ← Pages ← Custom domain، ثم سجل CNAME عند مزوّد النطاق. GitHub يوفّر شهادة HTTPS مجانًا (Enforce HTTPS).
- **يبقى قيد واحد على مستوى الجهاز:** آيفون في وضع توفير الطاقة يمنع التشغيل التلقائي لكل الفيديوهات — في هذه الحالة تظهر صورة الغلاف وزر تشغيل صغير.
- GitHub Pages للمواقع الثابتة فقط — وهذا ما نبنيه بالضبط، فلا مشكلة. نموذج الحجز يعمل عبر واتساب بلا خادم.

---

## 25 — جودة الفيديو على الاستضافة

**GitHub Pages (وأي استضافة عادية) لا تضغط الفيديو ولا تعيد ترميزه** — يُقدَّم الملف كما هو بايت ببايت، بعكس واتساب أو إنستقرام.

**ما يُعرض الآن (بعد تحديث اليوم):**

| الشاشة | الملف | الأبعاد | معدل البِت |
|---|---|---|---|
| ‏900px فأكثر | `intro-1080.mp4` | 1080×1920 | ‏2.06 Mbps |
| أقل من 900px | `intro-720.mp4` | 720×1280 | ‏0.88 Mbps |
| متصفح بلا H.264 | `intro-540.webm` | 540×960 | — |

الاختيار يتم عبر خاصية `media` على عناصر `<source>` (مختبَرة وتعمل)، فالجوال لا يحمّل نسخة الديسكتوب الثقيلة.

**قبل هذا التحديث كان الجميع يأخذ نسخة 720 حتى على الشاشات الكبيرة** — وهذا كان يظهر ناعمًا/مشوّشًا قليلًا على الديسكتوب لأن الفيديو يُمدَّد ليملأ الشاشة كاملة.

**حدود الجودة الحالية (مهم):**
- الملف الأصلي كان بترميز **HEVC** الذي لا تشغّله المتصفحات، فحُوّل إلى H.264 — وهذه الخطوة كلّفت بعض التفاصيل.
- المصدر تصوير جوال رأسي، ويُعرض ملء الشاشة، وفوقه طبقة لون بنفسجية بالتصميم.
- **للحصول على أعلى جودة ممكنة:** يلزم **الملف الأصلي من الكاميرا/الجوال مباشرة** (لا نسخة واتساب). عندها يمكن إخراج نسخة 1080 بمعدل بِت أعلى (3–4 Mbps) ونسخة 1440 للشاشات الكبيرة.
- المقايضة: كل زيادة في الجودة = ملف أكبر = بداية تشغيل أبطأ. السقف العملي لفيديو هيرو نحو 3 ميجا.

---

## 26 — تحديث V16 (٣٠ أغسطس ٢٠٢٦) — فيكتور الخلفية المتحرك

الفيكتور اللي بعتته العميلة (`1.svg` — حقنة بألوان صدفية، 426 مسار، 158KB، viewBox 724×2172 والرسم في ربعه الأعلى فقط).

**المعالجة:**
1. الـ`viewBox` اتقصّ على حدود الرسم الفعلية (2 682 670 848) → `assets/img/hero-syringe.svg`.
2. اتعمل منه **راستر شفاف مرة واحدة**: `assets/img/hero-syringe.webp` — 940×1190، **60 كيلوبايت** فقط. الراستر أرخص من الـSVG وقت الحركة لأن المتصفح ما بيعيدش رسم 426 مسار.

**الأداء — ده اللي اتقاس فعليًا (٣ تكرارات لكل حالة، سكرول ذهاب وإياب داخل نطاق محمّل):**

| الحالة | وسيط زمن الإطار | p95 | إطارات بطيئة |
|---|---|---|---|
| بدون الفيكتور (خط الأساس) | 16.6ms | 19.8ms | 2.7% |
| فيكتور بالإزاحة + الدوران | 16.7ms | 22.5ms | 9.5% |
| **فيكتور بالإزاحة فقط (المعتمد)** | **16.6ms** | **19.9ms** | **5.4%** |
| موبايل — المعتمد | 16.7ms | 18.3ms | 0.7% |

**الاستنتاج المهم: الدوران هو مصدر اللاج**، لأن تدوير طبقة كبيرة يجبر المتصفح على إعادة رسمها كل إطار. الإزاحة (`translate3d`) بتتنفّذ على المُركِّب (compositor) ببلاش.

**القواعد المطبَّقة:**
- الحركة `translate3d` **فقط** — أفقي ورأسي مع السكرول (`--gx` / `--gy`)، وطفو ذاتي بـ keyframes إزاحة على عنصر منفصل `.gem__f` مرقّى بـ `will-change:transform`.
- الميل ثابت (`rotate(-7deg)` على الصورة) — يُرسم مرة واحدة ولا يُحرَّك.
- ممنوع: `filter`، `box-shadow`، `scale` متحرك، أو أي قراءة تخطيط داخل `requestAnimationFrame` (أبعاد الصفحة تُقاس عند التحميل وعند `resize` فقط).
- `contain:layout paint` + `pointer-events:none` + `backface-visibility:hidden`.
- متوقف بالكامل مع `prefers-reduced-motion`.

الموضع: ثابت على جهة النهاية (يسار في RTL)، شفافية 26% في الوضع الفاتح و34% في الليلي، وأصغر وأبهت على الموبايل.

**تحديث ٣٠ أغسطس:** صورة قسم «عنا» اتغيّرت لصورة واجهة المجمع من الخارج (`assets/img/clinic/exterior.webp` — 960×1201، ‏115KB). الصورة القديمة (`clinic-1.6.jpg`) لسه موجودة في الأصول لو احتجناها في مكان تاني.

---

## 27 — تحديث V17 (٣٠ أغسطس ٢٠٢٦) — دوران الفيكتور + نافذة العروض

**الفيكتور بقى بيلف حول نفسه** بجانب حركته مع السكرول. الطبقات دلوقتي ثلاثة، كل واحدة ليها مهمة:
- `.gem` → إزاحة مع السكرول (JS، `translate3d` فقط)
- `.gem__f` → طفو ذاتي بطيء (keyframes إزاحة)
- `.gem__r` → **دوران كامل 360° في 48 ثانية** (`animation` خالص)

**ليه الدوران بقى بلا لاج المرة دي؟** لأنه اتنقل من JS (كان بيكتب `--gr` في كل إطار على الخيط الرئيسي) إلى **CSS animation** على طبقة مرقّاة — دي بتتنفّذ على المُركِّب (compositor) خارج الخيط الرئيسي تمامًا.

**القياس (٣ تكرارات لكل حالة):**

| الحالة | p95 | إطارات بطيئة |
|---|---|---|
| بدون الفيكتور | 20.6ms | 7.0% |
| **فيكتور يلف (المعتمد)** | **21.6ms** | **9.8%** |
| موبايل — يلف | 18.8ms | 1.4% |

⚠️ اتشال `contain:paint` من `.gem` — كان هيقص الحقنة أثناء الدوران لأنها مستطيلة والدوران بيوسّع صندوقها.

**نافذة العروض:**
- تظهر **بعد 1.5 ثانية** من فتح الصفحة، **مرة واحدة في الجلسة** (`sessionStorage`) — عشان ما تزهّقش الزائرة كل ما تفتح صفحة.
- الصورة نفسها رابط، وتحتها زر «تصفّحي العروض»، وزر «ليس الآن»، و× في الزاوية.
- تُغلق بـ Esc أو بالضغط على الخلفية، والتركيز يرجع لمكانه.
- الصور: `assets/img/offers/national-day-portrait.webp` (المستخدَمة) و`national-day-square.webp` (بديلة — تبديلها سطر واحد).

**صفحة العروض** موجودة في المشروع باسم `offers.html` (لاندنج اليوم الوطني اللي جهّزتها العميلة).

⚠️ **تحديث ٣٠ أغسطس:** العميلة بعتت نسخة جديدة بالأسعار المحدّثة، وطلبت إنها تتحط **كما هي بدون أي تعديل — بما فيها الخطوط**. فاتحطّت بالنص بايت ببايت، ورجعت تحمّل خط Zain من Google Fonts CDN. يعني **الصفحة دي بالتحديد مستثناة من قاعدة «الخطوط self-hosted»** الموجودة في القسم 6.

⚠️ **تنبيه:** صفحة العروض بتستخدم واتساب `966555474372` بينما باقي الموقع على `966112401164`. لازم نتفق على رقم واحد.

---

## 28 — تحديث V18 (٣٠ أغسطس ٢٠٢٦) — جهازان جديدان + صفحة لكل جهاز

**جهازان اتضافوا (المجموع 9):**
- **سيكريت RF** (`secret-rf`) — إبر مجهرية + ترددات راديوية.
- **السونار 2D و4D** (`sonar-voluson`) — متابعة الحمل ونمو الجنين.

صورهم اتقصّت من الخلفية البيضا واتحطّت في `assets/img/devices/`. ولأن صور الأجهزة عندنا نوعين (قصاصات شفافة وصور بخلفية بيضا)، بقى **إطار الصورة في كل بطاقات الأجهزة بلون فاتح موحّد** (`#F4F0F9`) — فالشكل متسق والصور البيضا ما بتبانش كمربع أبيض في الوضع الليلي.

**صفحة الجهاز:** الضغط على أي جهاز بيفتح عرضًا كاملًا فيه: صورة الجهاز، الاسم، الوصف المختصر، شرائح الاستخدامات، **فقرات شرح**، ثم **معرض «نتائج قبل وبعد»** بصور وتعليقات، وأزرار الحجز ورابط اللاندنج.

**من فين جت صور قبل/بعد للأجهزة السبعة القديمة؟** من لاندنج بيدجات العميلة نفسها. كل لاندنج ملف HTML واحد (1.4–1.8 ميجا) والصور جواه مضمّنة base64. اتسحبت الملفات من مستودعات GitHub عبر `raw.githubusercontent.com`، واتفصلت منها عناصر `figure.ba-item` بصورها و`figcaption` بتاعها:

| الجهاز | عدد صور قبل/بعد |
|---|---|
| ألثيرا | 3 |
| مورفيوس | 5 |
| أتيفا | 5 |
| ديكا | 5 |
| فراكشنال CO2 | 4 |
| ماجلان | 5 |
| سبلندر إكس | 4 |
| **سيكريت** (من العميلة) | 2 |
| **السونار** (صور توضيحية) | 4 |

كلها محفوظة في `assets/img/ba/` بعرض 900px WebP، والفهرس في `DEVDATA` داخل الجافاسكربت.

⚠️ **نص سيكريت اتكتب من جديد.** النص اللي وصل كان منقولًا من موقع عيادات **نوفومد** (فيه اسمهم ورقم هاتفهم 8006686) — نشره على موقع عيادة منافسة مشكلة حقوق نشر ومصداقية. الشرح الحالي مكتوب بصياغة أصلية وبنفس المعلومات الطبية العامة.

✅ **نص السونار** استُخدم كما أرسلته العميلة لأنه مكتوب أصلًا لعيادات د. إيمان العبرة، مع الاحتفاظ بملاحظة أن 4D ليس بديلًا عن الفحوصات الطبية.

**ملاحظة:** الجهازان الجديدان بلا لاندنج بيدج، فزر «تفاصيل أكثر» مخفي عندهم تلقائيًا.

---

## 29 — تحديث V19 (٣٠ أغسطس ٢٠٢٦) — حالات قبل/بعد جديدة + حذف سكشن العروض

**سكشن «أحدث العروض» اتشال** من الصفحة الرئيسية. رابط «العروض» في النافيجيشن بقى يوجّه لصفحة `offers.html` مباشرة. نافذة العروض المنبثقة لسه شغالة كما هي.

**تسع حالات جديدة اتضافت لسكشن «اسحبي الإبرة»** — **بعد** الحالات القديمة بنفس الترتيب (المجموع 17):

| # | الحالة | slug |
|---|---|---|
| 9 | محفزات الكولاجين | `collagen-boost` |
| 10 | ابتسامة هوليوود | `hollywood-smile` |
| 11 | توريد اللثة | `gum-depigment` |
| 12 | ندبات العمليات الجراحية | `surgical-scars` |
| 13 | الشد المائي | `hydro-lift-2` |
| 14 | بوتكس الجبهة | `forehead-botox` |
| 15 | فيلر فول بدي | `full-body-filler` |
| 16 | تفتيح الركب | `knees-brightening` |
| 17 | تفتيح الأكواع | `elbows-brightening` |

الملفات في `assets/img/results/` بنفس نمط التسمية `<slug>-before.jpg` و `<slug>-after.jpg` بعرض 900px.

**معالجتان خاصتان:**
- **توريد اللثة:** صورة «بعد» كانت **معكوسة أفقيًا** في الأصل (النص فيها مقلوب) — اتعدّلت لتطابق صورة «قبل».
- **ندبات العمليات:** وصلت كصورة واحدة فيها قبل وبعد جنب بعض — اتقسمت نصّين (اليسار = قبل، اليمين = بعد) عشان تشتغل مع سلايدر الإبرة.

**قائمة الحالات** بقت قابلة للتمرير رأسيًا على الشاشات الكبيرة (`max-height:min(74svh,600px)`) عشان 17 زر ما يطوّلوش السكشن أكتر من الصورة.

---

## 30 — تحديث V20 (٣٠ أغسطس ٢٠٢٦) — سكشن «لماذا عيادات د. إيمان العبرة»

الأيقونات الخطية القديمة (SVG داخلية) اتبدّلت بأيقونات العميلة الثلاثة بألوان الهوية (بنفسجي + ذهبي) في `assets/img/why/`:
`team.webp` · `results.webp` · `happy.webp` — كل واحدة اتقصّت من الخلفية البيضا (ألفا حسب بُعد البكسل عن الأبيض) واتحطّت في **مربع شفاف موحّد 360×360** عشان الثلاثة يظهروا بنفس المقاس البصري، وحجم العرض 84px.

النصوص الجديدة:
1. **فريق طبي ذو خبرة عالية**
2. **نتائج مبهرة ومناسبة**
3. **٩٩٪ من عميلاتنا راضيات** — عن الخدمة والنتائج

في الوضع الليلي بيتطبّق `filter:brightness(1.35) saturate(1.15)` على الأيقونات عشان الخطوط البنفسجية ما تغرقش في الخلفية الغامقة.

⚠️ **نقطتان تحتاجان قرار العميلة قبل النشر:**
1. **رقم ٩٩٪** — دي إحصائية قابلة للمساءلة في الإعلان الطبي السعودي. ربطناها بمصدر داخل النص («بحسب استبيان رضا العميلات في العيادة») لأن العيادة عندها استبيان رضا فعلي. **لازم يكون في بيانات فعلية تدعم الرقم**، وإلا يُستبدل بصيغة بلا رقم مثل «تقييمات ٥ نجوم على خرائط جوجل».
2. **«نتائج مبهرة»** — صيغة قريبة من الوعد بنتيجة. البديل الأأمن: «نتائج طبيعية ومناسبة لملامحك». القرار للعميلة.

---

## 31 — إضافة د. سلمى جعفر (٣٠ أغسطس ٢٠٢٦)

طبيبة سابعة اتضافت لقسم الطبيبات: **د. سلمى جعفر — استشارية أمراض النساء والتوليد والعقم**.

- الصورة اتقصّت من كارت «طبيبك» بقناع دائري (مركز 381,460 ونصف قطر 178 في الصورة الأصلية) بحواف ناعمة وخلفية شفافة → `assets/img/doctors/dr-salma-jaafar.webp` (368×368، 20KB).
- البيانات اتضافت في `content-manifest.json` وفي `DOCBIO` (٨ نقاط: ٢٥ عامًا خبرة، استشارية سابقة في مستشفى قوى الأمن ومستشفى د. سليمان الحبيب، جراحة المناظير النسائية المتقدمة، الحمل الحرج وعالي الخطورة، الإجهاض المتكرر، الأورام الليفية وأكياس المبيض، سلس البول وترميم الحوض، تشخيص وعلاج العقم).
- نص «لماذا نحن» اتحدّث من **ست طبيبات** لـ **سبع طبيبات**.

**ملاحظتان على النص الأصلي في الكارت:**
1. الصيغ كانت بالمذكر («استشاري سابق»، «أخصائي»، «خبير») — اتحوّلت للمؤنث اتساقًا مع باقي بطاقات الطبيبات.
2. الكارت بيكرّر «حالات الإجهاض المتكرر» ومتابعة الحمل مرتين — اتدمجوا في نقطتين بدل أربعة.

**تحديث نهائي:** العميلة بعتت البورتريه الأصلي (`PNG` بخلفية شفافة أصلًا) — فاتقصّ فوق البانر البنفسجي عند y=750، وحدود جسمها اتحسبت من قناة الألفا بعتبة 140 (عشان البكسلات الباهتة على الأطراف ما توسّعش الإطار)، والناتج `dr-salma-jaafar.webp` — 500×551، 62KB، مطابق تمامًا لأسلوب باقي البطاقات.

**درس للمرة الجاية:** المحاولتين الفاشلتين قبل كده (عزل لوني ثم GrabCut) كانوا على **صورة مقصوصة داخل دائرة** من كارت السوشيال — عند الكتفين مكانش فيه خلفية أصلًا، فأي تفريغ كان هيطلع كتلة مدوّرة. **القاعدة: نطلب البورتريه الأصلي من جلسة التصوير قبل أي محاولة تفريغ.**

---

## 32 — تحديث V21 (٣١ أغسطس ٢٠٢٦)

**١. الأقسام والخدمات بقت كاروسيل** زي الأطباء والأجهزة. الحاوية كانت `#railSrv` وأزرار التنقل موجودة أصلًا، بس البطاقات كانت لسه بقواعد `grid` فبتتمدّد وتملا العرض. الحل: `.srv__c{flex:0 0 auto;width:clamp(232px,72vw,290px);scroll-snap-align:start}` — دلوقتي بتتزحلق أفقيًا على كل المقاسات.

**٢. زر الحجز اللاصق بقى يفتح نافذة بيانات.** الموقع كان فيه `.fab-book` (تقويم بعلامة صح خضرا) بيعمل سكرول لسكشن الحجز فقط. دلوقتي بيفتح **نافذة `#bkm`** فيها: الاسم · رقم الجوال · الخدمة · الفرع · الوقت المناسب.
- تحقق من المدخلات (الاسم مطلوب، الجوال ≥ ٩ أرقام) برسائل عربية.
- عند الإرسال بيبني رسالة واتساب منسّقة ويفتحها، وبيقفل النافذة.
- Esc والضغط على الخلفية بيقفلوا، والتركيز بيرجع للزر.
- نسخة غامقة كاملة للوضع الليلي (بما فيها قوائم `option`).
- **مهم:** الزر لسه `<a href="#booking">`، فلو الجافاسكربت اتعطّل بيرجع يعمل سكرول للفورم الموجود في الصفحة — مفيش طريق مسدود.

**٣. ترتيب الأجهزة:** سيكريت والسونار اتنقلوا **لآخر** الريل بدل ما كانوا في الأول.

⚠️ ملاحظة: فيه دلوقتي **فورمين للحجز** — واحد في سكشن `#booking` وواحد في النافذة. المنطق متكرر في مكانين؛ لما نقسّم الموقع لصفحات، الأفضل نوحّدهم في دالة واحدة مشتركة.

---

## 33 — تحديث V22 (٣١ أغسطس ٢٠٢٦) — سكشن العروض رجع كبوسترات

سكشن `#offers` رجع تاني **بعد «لماذا عيادات د. إيمان العبرة»** مباشرة، بشكل مختلف عن الأول: مش كروت خدمات بأسعار، لكن **بوستران** من تصميمات العميلة (اليوم الوطني ٩٦) كل واحد منهم رابط كامل لصفحة `offers.html`.

- التصميمات: `assets/img/offers/national-day-portrait.webp` و `national-day-square.webp`.
- كل بوستر عليه إطار ذهبي رفيع، وعند الهوفر بيرتفع وتكبر الصورة قليلًا ويتمدّ خط الـ«التفاصيل والأسعار».
- شريط سفلي متدرّج على كل بوستر مكتوب فيه «التفاصيل والأسعار» — `pointer-events:none` عشان الضغط يروح للرابط نفسه.
- تحت البوسترين زر رئيسي «تصفّحي كل العروض».
- رابط «العروض» في النافيجيشن رجع يوجّه للسكشن `#offers` بدل ما كان بيفتح الصفحة مباشرة — الزائرة تشوف البوسترات الأول ثم تدخل للتفاصيل.
- التصميمان بنسبتين مختلفتين (1:1 و 4:5) فمتساويين من فوق ومختلفين من تحت (`align-items:start`) — شكل «حائط بوسترات» مقصود بدل قص الصور.

⚠️ **تنبيه:** التصميمان مكتوب فيهما **5500 ريال**، بينما `offers.html` اتحدّثت بأسعار جديدة. لازم تتأكد العميلة إن سعر البوستر مطابق للصفحة، وإلا تبعت تصميمات محدّثة — استبدالهم سطرين.
