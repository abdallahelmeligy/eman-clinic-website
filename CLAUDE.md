# CLAUDE.md — تعليمات المشروع

> اقرأ هذا الملف أولًا، ثم `HANDOFF.md` (السجل الكامل لكل قرار في المشروع).
> **لا تكتب أي كود قبل ما تعرض خطة ونتفق عليها.**

## ما هو هذا المشروع

موقع **عيادات د. إيمان العبرة** — مجمع طبي في الرياض (جلدية وتجميل وليزر · نساء وولادة · طب وجراحة أسنان).
الصفحة الرئيسية **مكتملة ومعتمدة من العميلة**: `index.html` — ملف واحد فيه الـHTML والـCSS والـJS.

**المطلوب منك: تقسيمها إلى موقع متعدد الصفحات جاهز للرفع.** التصميم مُقفل — مهمتك إعادة هيكلة، لا إعادة تصميم.

## قواعد لا تُكسر

1. **HTML / CSS / JS خام فقط.** لا React، لا Tailwind، لا npm، لا build step. الملفات تُرفع بالـFTP وتشتغل.
2. **صفحات حقيقية بروابط حقيقية** — لا تبويبات جافاسكربت. كل صفحة ملف `.html` مستقل بعنوانه ووصفه وبياناته المنظّمة.
3. **الهيدر والفوتر يتكرّران يدويًا** في كل صفحة. **ممنوع** حقنهما بالجافاسكربت (يضرّ الأرشفة ويسبب وميضًا عند التحميل).
4. **الخطوط مستضافة محليًا** من `assets/fonts/` عبر `assets/css/fonts.css`. لا Google Fonts CDN. **الاستثناء الوحيد: `offers.html`** — العميلة طلبت إبقاءها كما هي بخطوطها، لا تلمسها إطلاقًا.
5. **الموشن مكتوب بالإيد** — لا GSAP ولا AOS. الحركة `transform` و`opacity` فقط، ومعطّلة مع `prefers-reduced-motion`.
6. **ممنوع اختراع أي رقم أو إحصائية أو ادعاء طبي.** كل رقم في الموقع مصدره العميلة. لا تضف «نسبة نجاح» أو «أفضل عيادة» أو ما شابه.
7. **اللغة فصحى.** لا عامية مصرية. الخطاب موجّه للمؤنث (احجزي، شاهدي).
8. **أسماء الملفات بحروف صغيرة** — الاستضافة تفرّق بين الكبير والصغير على عكس ويندوز.
9. **الصور بصيغة WebP** وبأبعاد `width`/`height` مكتوبة لمنع قفز التخطيط.
10. **ممنوع تغيير الألوان أو الخطوط أو المسافات أو منطق الحركة.** أي اقتراح تحسين بصري: اذكره في رسالة، ولا تنفّذه.

## الوضع الحالي

```
index.html          الصفحة الرئيسية كاملة (HTML + CSS + JS داخلي)
offers.html         لاندنج العروض — من العميلة، لا تُعدَّل
content-manifest.json  مصدر الحقيقة: أطباء، أجهزة، فروع، خدمات، نتائج
HANDOFF.md          سجل كل قرار وسبب كل حل — اقرأه قبل أي تعديل
assets/
  css/fonts.css     تعريفات @font-face
  fonts/            woff2 مستضافة محليًا
  img/              doctors · devices · results · ba · depts · offers · clinic · why
  video/            intro-1080.mp4 · intro-720.mp4 · intro-540.webm · intro-poster.jpg
.nojekyll           لـGitHub Pages
```

## المطلوب

### المرحلة ١ — الفصل (ابدأ بها واعرض الخطة أولًا)
- `assets/css/main.css` — كل الـCSS من `index.html` كما هو، بدون إعادة كتابة ولا إعادة ترتيب.
- `assets/js/main.js` — كل الـJS كما هو.
- `assets/partials/` — الهيدر والفوتر كنص مرجعي **للنسخ اليدوي** في كل صفحة.
- تأكد أن `index.html` يعمل بعد الفصل **بشكل مطابق تمامًا** قبل أي خطوة تالية.

### المرحلة ٢ — الصفحات
```
index.html · about.html · services.html · doctors.html · devices.html
results.html · articles.html · contact.html · booking.html · 404.html
services/<slug>.html   (5 خدمات — النصوص من content-manifest.json)
doctors/<slug>.html    (7 طبيبات — البيانات من DOCBIO في main.js)
devices/<slug>.html    (9 أجهزة — الوصف ومعرض قبل/بعد من DEVDATA في main.js)
```
النوافذ المنبثقة الحالية للطبيبة والجهاز = **محتوى الصفحات الجديدة جاهز**. انقله كما هو.

### المرحلة ٣ — السيو والنشر
- `<title>` و`meta description` فريدان لكل صفحة + Open Graph.
- JSON-LD: `MedicalClinic` في الرئيسية، `Physician` لكل طبيبة، `MedicalDevice`/`Service` حسب الصفحة.
- `sitemap.xml` · `robots.txt` · `.htaccess` (صفحة 404، ضغط gzip، كاش للأصول).
- روابط داخلية متبادلة بين الصفحات.

## توحيد مطلوب أثناء العمل

- **فورما الحجز مكرّرتان** (واحدة في `#booking` وواحدة في نافذة `#bkm`) والمنطق منسوخ مرتين → وحّدهما في دالة واحدة داخل `main.js`.
- **رقم واتساب:** الموقع كله على `966112401164`، بينما `offers.html` على `966555474372`. اسأل العميلة عن الرقم المعتمد قبل التوحيد، ولا تغيّر `offers.html`.

## الاختبار قبل أي تسليم

```bash
python3 -m http.server 8000     # ثم افتح http://localhost:8000
```
تحقق يدويًا من:
- صفر أخطاء في الكونسول، وصفر تمرير أفقي على 375 و768 و1024 و1440.
- فيديو الهيرو يشتغل تلقائيًا ويتكرّر.
- الوضع الليلي يعمل في كل صفحة جديدة (زر القمر في الهيدر).
- كل الروابط الداخلية شغّالة بعد التقسيم.
- نموذج الحجز يبني رسالة واتساب صحيحة.

## أشياء مقرّرة لا تُناقَش

انظر `HANDOFF.md` — كل بند فيه له سبب مكتوب. أهمها:
- الإبرة هي مقبض سلايدر قبل/بعد.
- الفيكتور في الخلفية يتحرك بـ`translate` فقط؛ **الدوران بالـJS يسبب لاج** (مقيس)، والدوران المسموح به عبر CSS animation على طبقة مستقلة.
- صور الطبيبات والأجهزة = القصاصات النظيفة، لا نسخ `-full` (فيها رقم هاتف قديم محروق في الصورة).
